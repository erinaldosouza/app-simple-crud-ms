server:
  port: 8080

management:
  endpoints:
      web:
        exposure:
          include: info,health
spring:
  application:
    name: ms-device-matching


x_aerospike:
  host: localhost
  port: 300
  namespace: device-matching
